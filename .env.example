# verbo.dev — variáveis de ambiente
# Copie este arquivo para .env e preencha os valores
# NUNCA commite o arquivo .env

# Nenhuma variável sensível é necessária para o core do verbo.
# Este arquivo existe apenas para extensões futuras (ex: sync de progresso opt-in).

# VERBO_SYNC_ENDPOINT=
# VERBO_USER_TOKEN=
